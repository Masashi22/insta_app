<%= provide(:title, "投稿一覧") %>
<h1>投稿一覧</h1>
<div class="micropost-content">
  <% @microposts.each do |micropost| %>
    <div id="pictures">
      <%= link_to image_tag(micropost.picture.url, alt: "main_image"), micropost_path(micropost) %>
    </div>
  <% end %>
</div>
