<%= provide(:title, "全ユーザー") %>
<h1>ユーザー一覧</h1>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= image_tag user.gravatar_url %>
      <%= user.username %>
    </li>
  <% end %>
</ul>
